name: PR File Check

on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  check-files:
    runs-on: ubuntu-latest

    steps:
      - name: Immutable Files
        uses: asafHalely/immutable-file-action@v1.1
        with:
          files: requirements.txt
